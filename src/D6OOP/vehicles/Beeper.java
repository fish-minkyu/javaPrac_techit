package D6OOP.vehicles;
// interface
// : 기본적으로 추상 메서드들을 모으기 위해 만들어진 기능이다.
// 추상 메서드만 존재하는게 interface이다.
public interface Beeper { // 변수를 가질 수 없고, 기능이 존재하는 메서드들은 만들 수는 있다.
  // interface의 핵심은 추상 메서드만 모으는 것이다.
  void beep();
}
